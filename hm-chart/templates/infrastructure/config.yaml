apiVersion: v1
kind: ConfigMap
metadata:
  name: hm-config
data:
  QDRANT_HOST: "qdrant-service"
  REDIS_HOST: "redis-service"
  
  BACKEND_URL: "http://backend-service:{{ .Values.backend.service.port }}"
  
  GF_SECURITY_ADMIN_PASSWORD: {{ .Values.config.grafanaPassword | quote }}